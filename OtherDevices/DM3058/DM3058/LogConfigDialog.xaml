<Window x:Class="DM3058.LogConfigDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DM3058"
        mc:Ignorable="d"
        Title="Logging Configuration" Height="250" Width="600" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Log File Path -->
        <Grid Grid.Row="0" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Log File Path:" VerticalAlignment="Center" Margin="0,0,10,0" FontSize="14"/>
            <TextBox Grid.Column="1" Name="txtLogPath" FontSize="14" Padding="5" VerticalContentAlignment="Center"/>
            <Button Grid.Column="2" Name="btnBrowse" Content="Browse..." Click="btnBrowse_Click" Margin="10,0,0,0" Padding="15,5" FontSize="14"/>
        </Grid>

        <!-- Log Format -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Log Format:" VerticalAlignment="Center" Margin="0,0,10,0" FontSize="14"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <RadioButton Name="rbCSV" Content="CSV" GroupName="format" IsChecked="True" Margin="0,0,20,0" VerticalAlignment="Center" FontSize="14"/>
                <RadioButton Name="rbXML" Content="XML" GroupName="format" VerticalAlignment="Center" FontSize="14"/>
            </StackPanel>
        </Grid>

        <!-- Info Text -->
        <TextBlock Grid.Row="2" TextWrapping="Wrap" Margin="0,0,0,15" FontSize="12" Foreground="Gray">
            <Run Text="Note: The log file will be created when logging starts. If the file already exists, new data will be appended."/>
        </TextBlock>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Name="btnOK" Content="OK" IsDefault="True" Click="btnOK_Click" MinWidth="80" Padding="5" Margin="0,0,10,0" FontSize="14"/>
            <Button Name="btnCancel" Content="Cancel" IsCancel="True" MinWidth="80" Padding="5" FontSize="14"/>
        </StackPanel>
    </Grid>
</Window>
